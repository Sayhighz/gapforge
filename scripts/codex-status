#!/bin/sh
set -eu

if command -v docker-compose >/dev/null 2>&1; then
    exec docker-compose --profile auth run --rm codex-status "$@"
fi
exec docker compose --profile auth run --rm codex-status "$@"
